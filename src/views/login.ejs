<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/auth.css">
</head>
<body>
    <header>
        <img src="/images/logo-senai.png" alt="Logo">
    </header>
    <div class="container">
        <h1>Login</h1>
        <h4>Bem vindo ao PPC</h4>
        
        <% if (typeof error !== 'undefined') { %>
            <p style="color: red;"><%= error %></p>
        <% } %>
        <form id="loginForm" action="/user/login" method="POST" class="form">
            <div class="form-group">
                <label for="email">Usuario</label>
                <input class="form-control" type="email" id="email" name="email" required placeholder="Insira Seu Email">
            </div>
            <div class="form-group">
                <label for="password">Senha</label>
                <input class="form-control" type="password" id="password" name="password" required placeholder="Insira Sua Senha">
            </div>
            <button type="submit">Entrar</button>
        </form>
        <p>Não tem uma conta? <a href="/user/signup">Registre-se</a></p>
    </div>
    <footer>
        © SENAI-SP - 2024
    </footer>
</body>
</html>
